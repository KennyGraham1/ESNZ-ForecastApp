# Read the Docs / local docs build dependencies (Sphinx + classic RTD theme).
# Pinned to the tested, mutually-compatible set so the hosted build reproduces
# the verified local build exactly. Markdown is parsed with MyST; diagrams via
# sphinxcontrib-mermaid (native d3 zoom + fullscreen).
sphinx==7.4.7
sphinx-rtd-theme==3.1.0
myst-parser==3.0.1
sphinxcontrib-mermaid==1.2.3
